Ты senior frontend engineer. Работай сразу в коде текущего проекта.Цель:Встроить видео в сайт с кинематографичным эффектом:1) первый экран — full-screen видео-фон,2) второй экран — видео, управляемое скроллом (scrub),3) переход между экранами — плавный и бесшовный.Входные данные (замени значения):- Stack: [auto-detect from package.json]- Hero video path: [VIDEO_HERO_PATH]- Scroll video path: [VIDEO_SCROLL_PATH]- Fallback/poster image: [FALLBACK_IMAGE_PATH or generate]- UI language: [LANG]- Visual direction: [STYLE_NOTES]Обязательные требования:- Не добавляй новые библиотеки без необходимости.- Не ломай существующий дизайн-системный стиль проекта.- Не делай лишний рефакторинг вне задачи.- Адаптив обязателен (desktop + mobile).- Используй безопасную высоту (`100dvh`), чтобы избежать мобильных скачков.Hero (экран 1):- Видео занимает весь экран без рамок и артефактов.- Поверх только контент/CTA, с читаемостью на любом кадре.- Если композиция требует, разнеси контент по сторонам (left/right), чтобы центр оставался под ключевой визуальный объект.- Ничего не должно обрезаться у нижней кромки (кнопки/текст/чипы).Scroll section (экран 2):- Видео остается фоном (sticky/fixed), а текст движется поверх по скроллу.- Скролл управляет `currentTime` видео плавно.- Не обновляй `currentTime` напрямую на каждый scroll tick.- Используй схему: `targetTime` + `requestAnimationFrame` + time-based easing (dt-aware interpolation).Производительность и плавность:- Минимизируй тяжелые эффекты в реальном времени (особенно динамические filter/blur на видео).- Анимируй в основном transform/opacity.- При рывках подготовь scrub-friendly видео-версию:  - all-keyframes,  - faststart,  - разумное разрешение для веба.- Подключи оптимизированный файл вместо исходного при необходимости.Переход между экранами (критично):- Убери швы, серые полосы, резкие линии и “ступени”.- Сделай мягкий визуальный переход (mask/gradient/crossfade).- Если два разных ролика визуально конфликтуют, реализуй общий фон-слой и кроссфейд между видео в зоне стыка.Читаемость UI:- Текст должен быть контрастным на темных и светлых кадрах.- Короткие преимущества/бейджи делай в контрастных плашках, а не просто белым текстом по видео.- Проверяй реальную читаемость на нескольких кадрах ролика.Надежность:- Обработай `loadedmetadata`, `canplay`, `error`.- Используй fallback/poster до готовности видео.- Не допускай “пустого” состояния секции при ошибке загрузки видео.Что вернуть в конце:1) Список измененных файлов.2) Краткое описание, что и почему сделано.3) Как запустить и проверить локально.4) Результат проверки сборки (`npm run build` или эквивалент).5) Быстрый QA-чек:   - нет шва между экранами,   - нет обрезанных кнопок/текста,   - видео второго экрана двигается плавно,   - mobile не ломается.