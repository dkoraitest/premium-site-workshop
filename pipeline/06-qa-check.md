# Этап 6: QA-проверка

Финальная проверка перед security-аудитом и деплоем. Всё, что может сломаться у посетителя.

---

## Чеклист

### Контент

- [ ] Нет lorem ipsum, placeholder, «ваш текст здесь», TODO
- [ ] Все заголовки и описания — реальный контент
- [ ] Контактные данные актуальные (телефон, email, адрес)
- [ ] Copyright в футере с правильным годом и названием
- [ ] Юридическая информация на месте (ИНН, ООО/ИП)

### Ссылки и навигация

- [ ] Все ссылки работают (нет 404, нет `href="#"`)
- [ ] Якорные ссылки в навигации прокручивают к нужной секции
- [ ] Smooth scroll работает (Lenis или CSS scroll-behavior)
- [ ] Burger menu открывается и закрывается на мобильных
- [ ] Меню закрывается при клике на ссылку
- [ ] Телефоны кликабельны (`tel:+7...`)
- [ ] Email кликабелен (`mailto:...`)
- [ ] Ссылки на соцсети открываются в новой вкладке (`target="_blank" rel="noopener"`)
- [ ] Логотип в хедере ведёт на главную

### Изображения

- [ ] Все изображения загружаются (нет broken images)
- [ ] Все `<img>` имеют `alt` (для accessibility и SEO)
- [ ] Все `<img>` имеют `width` и `height` (для предотвращения CLS)
- [ ] Изображения в WebP (или сжатом JPEG, < 200KB)
- [ ] Hero-изображение загружается быстро (fetchpriority="high")
- [ ] og:image существует и корректного размера

### Формы

- [ ] Форма отправляется — уведомление приходит в Telegram
- [ ] Валидация работает: пустые обязательные поля → ошибка
- [ ] Невалидный email → ошибка
- [ ] При отправке кнопка блокируется (нет double-submit)
- [ ] Сообщение об успехе отображается
- [ ] Сообщение об ошибке отображается (если сервер недоступен)
- [ ] Форма не ломается на мобильных

### Responsive

- [ ] **iPhone SE (375px):** все секции видны, нет горизонтального скролла
- [ ] **iPhone SE (375px):** текст читаем, не слишком мелкий (min 14px)
- [ ] **iPhone SE (375px):** кнопки достаточно крупные (min 44x44px touch target)
- [ ] **iPhone SE (375px):** burger menu работает
- [ ] **iPad (768px):** layout адаптируется (не растянутый мобильный и не сжатый десктоп)
- [ ] **Desktop (1440px):** все секции выглядят как задумано
- [ ] **Wide screen (1920px+):** контент не растягивается на всю ширину (max-width)
- [ ] Нет горизонтального скролла ни на одном размере экрана

### Cross-browser

- [ ] Chrome — всё работает
- [ ] Safari — всё работает (особенно backdrop-filter, scroll-driven animations)
- [ ] Firefox — всё работает (проверить GSAP, clip-path)
- [ ] Safari iOS — проверить на реальном iPhone (если есть)

### Анимации и производительность

- [ ] Анимации плавные (60fps, нет jank)
- [ ] Scroll-driven анимации не дёргаются
- [ ] Hover-эффекты отзывчивые (нет задержки)
- [ ] Кастомный курсор (если есть) следит за мышью без лагов
- [ ] Marquee (если есть) не дёргается, бесшовный цикл
- [ ] На мобильных тяжёлые анимации упрощены или отключены
- [ ] Hero-видео (если есть) играет, не зависает, poster-кадр виден до загрузки
- [ ] Lazy loading работает для контента ниже fold
- [ ] Нет неиспользуемых скриптов/стилей (проверить Network tab)

### SEO (быстрая проверка)

- [ ] `<title>` задан и уникален
- [ ] `<meta name="description">` задан
- [ ] `<link rel="canonical">` задан
- [ ] JSON-LD schema в head (проверить: нет ошибок в консоли)
- [ ] Open Graph теги заданы
- [ ] Favicon отображается в табе браузера

---

## Промпт для автоматизированной QA-проверки

> Проведи полную QA-проверку сайта. Открой index.html и проверь каждый пункт:
>
> **Контент:**
> - Найди все строки с "lorem", "ipsum", "placeholder", "TODO", "ваш текст", "example" — если найдены, перечисли где.
> - Проверь что в футере есть год 2026 и название компании.
>
> **Ссылки:**
> - Найди все `<a href="...">` — проверь что ни один href не ведёт в никуда (не `#`, не пустой, не `javascript:void`).
> - Якорные ссылки (href="#section") — проверь что элементы с этими id существуют.
> - Внешние ссылки — проверь что имеют `target="_blank" rel="noopener"`.
>
> **Изображения:**
> - Найди все `<img>` — проверь что файлы существуют, alt задан, width/height заданы.
> - Проверь что изображения ниже fold имеют `loading="lazy"`.
>
> **Responsive:**
> - Проверь CSS: найди все элементы с `position: fixed/absolute`, `width` в пикселях на корневых контейнерах, `overflow-x` — потенциальные причины горизонтального скролла.
> - Проверь что есть media queries для 375px, 768px, 1024px+.
> - Проверь что burger menu существует и стилизован для мобильных.
>
> **Формы:**
> - Найди все `<form>` — проверь что есть action или JS-обработчик.
> - Проверь что обязательные поля имеют `required` или JS-валидацию.
> - Проверь что кнопка submit имеет обработчик disable при отправке.
>
> **Анимации:**
> - Проверь что GSAP/Lenis подключены с конкретными версиями (не latest).
> - Проверь что ScrollTrigger анимации используют `will-change` или `transform` (не left/top).
> - Проверь что на тач-устройствах кастомный курсор скрыт (media query `pointer: coarse` или `hover: none`).
>
> **Performance:**
> - Подсчитай общий размер JS и CSS (сколько файлов, примерный объём).
> - Проверь что видео имеет `preload="none"` или `preload="metadata"` (кроме hero).
> - Проверь что нет `console.log` в production-коде.
>
> Результат оформи как таблицу: | Проверка | Статус | Комментарий |
> Где статус: PASS / FAIL / WARN.

---

## Промпт: Исправление найденных проблем

> На основе QA-аудита из `AUDIT_QA.md` исправь все найденные проблемы:
>
> 1. Все FAIL-пункты — исправить обязательно.
> 2. Все WARN-пункты — исправить если это не влияет на дизайн.
> 3. После исправлений — пройди чеклист заново и подтверди что всё PASS.

---

## Типичные проблемы и решения

### Горизонтальный скролл на мобильных

Причины:
- Элемент с `width` больше viewport (часто — hero текст, изображение, marquee)
- `padding` или `margin` выталкивают за viewport
- `position: absolute` / `fixed` с `right: -20px`

Решение:
```css
/* Добавь на body или html */
html, body {
  overflow-x: hidden;
}

/* Но лучше найти и исправить конкретный элемент! */
/* overflow-x: hidden — это костыль, не решение */
```

### Анимации дёргаются (jank)

Причины:
- Анимация свойств `width`, `height`, `left`, `top` (вместо `transform`)
- Нет `will-change: transform` на анимируемых элементах
- Слишком много одновременных анимаций

Решение:
```css
/* Вместо */
.card { left: 0; transition: left 0.3s; }
.card:hover { left: 10px; }

/* Используй */
.card { transform: translateX(0); transition: transform 0.3s; will-change: transform; }
.card:hover { transform: translateX(10px); }
```

### Safari не показывает backdrop-filter

```css
/* Добавь webkit-префикс */
.glass-card {
  -webkit-backdrop-filter: blur(20px);
  backdrop-filter: blur(20px);
}
```

### Lenis + ScrollTrigger конфликтуют

Убедись что:
1. Lenis инициализирован ДО ScrollTrigger
2. Используется скилл orchestrating-gsap-lenis или ручная интеграция:
```js
lenis.on('scroll', ScrollTrigger.update);
gsap.ticker.add((time) => { lenis.raf(time * 1000); });
gsap.ticker.lagSmoothing(0);
```
